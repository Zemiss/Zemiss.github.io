---
layout:     post
title:      "实变函数-第二章"
subtitle:   " \"实变函数学十遍QwQ\""
date:       2026-04-17 9:45:00
author:     "Zemiss"
header-img: "img/bg-RA.jpg"
catalog: true
mathjax: true
tags:
  - RA
---

# 实变函数-第二章 勒贝格测度

2

求证: $$m^*(E) = \inf\left\{ m(Q) : E \subset Q, Q 是开集\right\}$$

**思路：大于等于、小于等于**

3

设 $E \subset \mathbb{R}, M > 0$. 

求证: $$m^*(E) = \inf\left\{ \sum \ell(I_n) : I_n是开区间,\ell(I_n) < M, E \subset \bigcup_{n=1}^\infty I_n \right\}$$.

---

**思路：大于等于、小于等于**

4

设 $G_1$ 和 $G_2$ 是不相交开集, $E_1 \subset G_1, E_2 \subset G_2$, 

求证: $$m^*(E_1 \cup E_2) = m^*(E_1) + m^*(E_2)$$

---

**思路：$m^*(E_1\cup E_2)=m^*((E_1\cup E_2)\cap G_1)+m^*((E_1\cup E_2)\cap G_1^c)$**

5

若 $d(E_1, E_2) = \inf\{d(x_1, x_2) : x_1 \in E_1, x_2 \in E_2\} > 0$, 

求证: $$m^*(E_1 \cup E_2) = m^*(E_1) + m^*(E_2)$$

---

**思路：同上**

8

设对每一 $x \in I = (a,b)$, $A_x$ 是一个实数集, 而且当 $x_1 < x_2$ 时 $A_{x_1} \subset A_{x_2}$. 

求证:

(i) 

$$m^*\left( \bigcup_{x \in I} A_x \right) = \lim_{x \to b^-} m^*(A_x)$$

(ii) 

$$m^*\left( \bigcap_{x \in I} A_x \right) \le \lim_{x \to a^+} m^*(A_x)$$

(iii) 当 $A_x$ 可测时 

$$m\left( \bigcup_{x \in I} A_x \right) = \lim_{x \to b^-} m(A_x)$$

此外当有某个 $A_x$ 测度有限时,$$m\left( \bigcap_{x \in I} A_x \right) = \lim_{x \to a^+} m(A_x)$$.

9

设 $E \subset \mathbb{R}, 0 < m^*(E) < \infty$, 

**求证: $f(x) = m^*((-\infty, x) \cap E)$ 是 $x$ 的连续函数.**

**求证:$I = \{m^*(F) : F \subset E\}$ 是一个有界闭区间.**

10

设 $\{E_n\}_{n \ge 1}$ 是可测集列,

(i) 求证 

$$m\left( \varliminf_{n \to \infty} E_n \right) \le \varliminf_{n \to \infty} m(E_n)$$

(ii) 若有 $k_0$ 使 $$m\left( \bigcup_{k=k_0}^\infty E_k \right) < \infty$$

求证:

$$m\left( \varlimsup_{n \to \infty} E_n \right) \ge \varlimsup_{n \to \infty} m(E_n)$$

(iii) 若 $$m\left( \bigcup_{k=1}^\infty E_k \right) < \infty$$ 且 $\lim_{n \to \infty} E_n$ 存在, 

求证:

$$m\left( \lim_{n \to \infty} E_n \right) = \lim_{n \to \infty} m(E_n)$$

12

设 $0 < m(E) < \infty$. 

**求证: 有测度皆为 $m(E)$ 的开集列 $\{G_n\}_{n \ge 1}$, 使 $m(E \triangle G_n) \to 0\ (n \to \infty)$.**

---

15

(i) 若 $F$ 是 $[0,1]$ 中闭集且 $m(F)=1$. 试问是否一定 $F=[0,1]$?

(ii) 若 $G$ 是 $(0,1)$ 中开集且 $m(G)=1$, 试问是否一定 $G=(0,1)$?

---

**思路：补集**

17

设 $E \subset \mathbb{R}, m(E) > 0, 0 < \alpha < 1$. 

**求证: 有开区间 $I$ 使 $m(I \cap E) > \alpha \cdot m(I)$.**

---

**思路：反证**

21

设$$\{E_k\}_{k \ge 1}$$ 是 $[0,1]$ 中的可测集列, 使得 $m(E_k) \to 1\ (k \to \infty)$.

**求证: 对任何 $0 < \lambda < 1$, 有子列 $$\{E_{k_n}\}_{n \ge 1} $$
使 $$m\left( \bigcap_{n=1}^\infty E_{k_n} \right) > \lambda$$**

---

**思路：$m(E_{k_n}^c)<\frac{1-\lambda}{2^n}.$**


23

设 $$\{I_\lambda\}_{\lambda \in \Lambda}$$ 是一族区间, 

求证: $E = \bigcup_{\lambda \in \Lambda} I_\lambda$ 可测

---

**思路：任意多个区间的并，可以合并成至多可数个互不相交区间的并。**


26

设 $A \cup B$ 可测且 $$m(A \cup B) = m^*(A) + m^*(B) < \infty$$. 

**求证: $A$ 和 $B$ 都可测.**

(提示: 取 $G_\delta$ 集 $H$ 使 $$H \supset B, m(H) = m^*(B)$$此时可证 $$(A \cup B) \cap H^c$$ 是 $A$ 的可测子集, 其测度为 $m^*(A)$.)

---

**思路：若 A 含有可测子集 C, $m(C)=m^*(A)$，A 可测。**


27

构造不相交的集 $A$ 和 $B$ 使 $$m^*(A \cup B) < m^*(A) + m^*(B)$$

**思路：构建不可测的集合**

28

设 $E \subset \mathbb{R}, m(E) > 0$. 令
$$E^* = \{x \in E: 对任何 \delta > 0 有 m(E \cap (x-\delta, x+\delta)) > 0\}$$.

**求证: $E^*$ 可测且 $$m(E^*) = m(E)$$**

**思路：补集测度为零**


## 外测度平移不变性与伸缩性

29

**条件：设$E \subset \mathbb{R}$可测，$a$和$b$是两个实数。**

**求证：$F = \lbrace ax + b: x \in E\rbrace $可测并且$m(F) = \left\vert a \right\vert \cdot m(E)$**

---
设$F={ax+b:x\in E}=aE+b$。

下面直接利用 Lebesgue 外测度 $m^*$ 的平移不变性与伸缩性：

$$
\boxed{m^*(A+b)=m^*(A),\qquad m^*(aA)=\left\vert a \right\vert m^*(A)\quad(A\subset \mathbb R).}
$$

---

当 $a=0$ 时，$F={b},$显然可测，且$m(F)=0=\left\vert a \right\vert m(E)$ 故结论成立。

设 $a\neq 0$，任取 $A\subset \mathbb R$。由于 $E$ 可测，由 Carathéodory 判据有

$$m^*\left(\frac{1}{a}A\right)=m^*\left(\frac{1}{a}A\cap E\right)+m^*\left(\frac{1}{a}A\cap E^c\right).$$

两边乘以 $|a|$，得


$$\left\vert a \right\vert \,m^*\left(\frac{1}{a}A\right)
=\left\vert a \right\vert \,m^*\left(\frac{1}{a}A\cap E\right)
+\left\vert a \right\vert \,m^*\left(\frac{1}{a}A\cap E^c\right).$$


由外测度的伸缩性质，

$$
m^*(A)=\left\vert a \right\vert\,m^*\left(\frac{1}{a}A\right).
$$

又

$$
m^*(A\cap aE)=\left\vert a \right\vert\,m^*\left(\frac{1}{a}(A\cap aE)\right)
=\left\vert a \right\vert\,m^*\left(\frac{1}{a}A\cap E\right),
$$

同理

$$
m^*(A\cap (aE)^c)=
\left\vert a \right\vert\,m^*\left(\frac{1}{a}(A\cap (aE)^c)\right)
=\left\vert a \right\vert\,m^*\left(\frac{1}{a}A\cap E^c\right).
$$

这里用到了

$$
(aE)^c=a(E^c)\qquad (a\neq 0),
$$

因为映射 $x\mapsto ax$ 是 $\mathbb R$ 上的双射。

因此

$$
m^*(A)=m^*(A\cap aE)+m^*(A\cap (aE)^c).
$$

由 Carathéodory 判据，$aE$ 可测。

---

既然 $F$ 可测，就有

$$
m(F)=m^*(F)=m^*(aE+b).
$$

再由外测度的平移不变性与伸缩性，

$$
m(F)=m^*(aE+b)=m^*(aE)=\left\vert a \right\vert m^*(E)=\left\vert a \right\vert m(E).
$$

## Tonelli 定理和 Steinhaus 定理

32

**条件：设$m(A) > 0, m(B) > 0$，**

**求证：$\lbrace a - b: a \in A, b \in B\rbrace$及$\lbrace a + b: a \in A, b \in B\rbrace $都有内点。**

---


考虑函数

$$
f(x)=m\bigl(A\cap (B+x)\bigr),
$$

其中

$$
B+x=\{b+x:b\in B\}.
$$

**由 Tonelli 定理**，有

$$
\begin{aligned}
\int_{\mathbb R} f(x)\,dx
&=\int_{\mathbb R} m\bigl(A\cap(B+x)\bigr)\,dx\\
&=\int_{\mathbb R}\int_{\mathbb R}\mathbf{1}_A(t)\mathbf{1}_{B+x}(t)\,dt\,dx\\
&=\int_{\mathbb R}\int_{\mathbb R}\mathbf{1}_A(t)\mathbf{1}_B(t-x)\,dt\,dx\\
&=\int_{\mathbb R}\mathbf{1}_A(t)\left(\int_{\mathbb R}\mathbf{1}_B(t-x)\,dx\right)dt\\
&=\int_{\mathbb R}\mathbf{1}_A(t)\,m(B)\,dt\\
&=m(A)m(B)>0.
\end{aligned}
$$

因此存在某个 $x\in\mathbb R$，使得

$$
m\bigl(A\cap(B+x)\bigr)>0.
$$

记

$$
E=A\cap(B+x).
$$
则

$$
m(E)>0.
$$

**由 Steinhaus 定理**，集合

$$
E-E=\{u-v:u,v\in E\}
$$

包含 $0$ 的某个开邻域。也就是说，存在 $\varepsilon>0$，使得

$$
(-\varepsilon,\varepsilon)\subset E-E.
$$

下面证明

$$
x+(E-E)\subset A-B.
$$

任取 $u,v\in E$。由于 $u\in E\subset A$，且 $v\in E\subset B+x$，故存在 $b\in B$，使得

$$
v=b+x.
$$

于是

$$
x+(u-v)=x+u-(b+x)=u-b.
$$

因为 $u\in A,\ b\in B$，所以

$$
u-b\in A-B.
$$

从而

$$
x+(E-E)\subset A-B.
$$

又由于

$$
(-\varepsilon,\varepsilon)\subset E-E,
$$

故

$$
x+(-\varepsilon,\varepsilon)\subset x+(E-E)\subset A-B.
$$

这说明 $A-B$ 包含开区间

$$
(x-\varepsilon,\ x+\varepsilon),
$$
因此 $A-B$ 有内点。

---

令

$$
B'=-B=\{-b:b\in B\}.
$$

由于 Lebesgue 测度在取负下不变，所以

$$
m(B')=m(B)>0.
$$

而且

$$
A+B=A-(-B)=A-B'.
$$

由第一部分已经证明的结论，两个正测度可测集之差有内点，因此

$$
A-B'=A+B
$$

有内点。

## 反证补集

34

**条件：$(0,1)$中的数$x$用十进制表示，$x_n$是其第$n$位小数。令$A_9 = \lbrace x \in (0,1): \max\lbrace x_n\rbrace = 9\rbrace$。**

**求证：$m(A_9)=1$。**

---

**思路：补集测度为零测**

---

37

**条件：设$F$是$[0,1]$中不可测集。**

**求证：有$0 < \varepsilon < 1$，使对$[0,1]$中任何满足$m(E) \ge \varepsilon$的可测集$E$，$F \cap E$也是不可测集。**

---

**思路：反证，$F=(F\cap E)\cup\bigl(F\cap([0,1]\setminus E)\bigr)$**


## 零测集

38

**条件：设$f(x)$定义在$\mathbb{R}$上，并且对任何可测集$E$，$f(E)$可测。**

**求证：对任何零测集$Z$，$f(Z)$也是零测集。**

---

**思路：反证，任何正测度可测集都含有不可测子集**



## 逆函数

40

**条件：设$f(x)$在$\mathbb{R}$上连续可微且$f'(x) > 0$。**

**求证：当$E$可测时，$f^{-1}(E)$也可测。**

---

**思路：$E = B \cup N$**

1.  **$f^{-1}(B)$ 的性质**：
    由于 $f$ 是连续函数，Borel 集的原像仍是 Borel 集。而所有 Borel 集都是 Lebesgue 可测的
2.  **$f^{-1}(N)$ 的性质**：
    由第一步的证明可知，零测集的原像 $f^{-1}(N)$ 仍是零测集。因为零测集必然是 Lebesgue 可测的

